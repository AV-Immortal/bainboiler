# contact-cta

- Page: homepage
- Purpose: finish the homepage with an embedded inquiry form and a secondary download conversion action
- CMS fields: `eyebrow`, `title`, `description`, `primaryCta` (submit label), `secondaryCta` (download link label)
- Replace this module by editing only `src/modules/contact-cta`
